<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".activity.MainActivity">

    <FrameLayout
        android:id="@+id/fl_wrapper"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation" />

    <!--    <com.google.android.material.bottomnavigation.BottomNavigationView-->
    <!--        android:id="@+id/bottom_navigation"-->
    <!--        android:layout_width="match_parent"-->
    <!--        android:layout_height="80dp"-->
    <!--        android:layout_alignParentBottom="true"-->
    <!--        android:background="@color/black"-->
    <!--        android:elevation="0dp"-->
    <!--        app:itemIconTint="@color/selector_bottom_tab_item"-->
    <!--        app:itemTextColor="@color/selector_bottom_tab_item">-->

    <LinearLayout
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:padding="5dp">

        <TextView
            android:id="@+id/tvTransactions"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:drawableTop="@drawable/ic_wallet"
            android:drawableTint="@color/selector_bottom_tab_item"
            android:gravity="center"
            android:text="@string/transactions"
            android:textColor="@color/selector_bottom_tab_item" />

        <TextView
            android:id="@+id/tvAccount"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:drawableTop="@drawable/ic_account"
            android:drawableTint="@color/selector_bottom_tab_item"
            android:gravity="center"
            android:text="@string/account"
            android:textColor="@color/selector_bottom_tab_item" />
    </LinearLayout>
    <!--    </com.google.android.material.bottomnavigation.BottomNavigationView>-->

    <ImageButton
        android:id="@+id/btnAddTransactions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:backgroundTint="@android:color/transparent"
        android:elevation="10dp"
        android:src="@drawable/add_transactions" />


</RelativeLayout>